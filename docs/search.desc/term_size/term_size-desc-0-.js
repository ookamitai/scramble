searchState.loadedDescShard("term_size", 0, "System calls for getting the terminal size.\nQuery the current processes’s output (<code>stdout</code>), input (…\nQuery the current processes’s error output (<code>stderr</code>) <em>only</em>…\nQuery the current processes’s input (<code>stdin</code>) <em>only</em>, in the …\nQuery the current processes’s output (<code>stdout</code>) <em>only</em>, in …")